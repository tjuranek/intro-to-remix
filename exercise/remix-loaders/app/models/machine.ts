import { prisma } from '~/db.server';

/**
 * Prisma is available under the imported prisma object.
 *
 * Export a function that gets all the machines.
 *
 * Export a function that takes a machine id and returns just that machine.
 */
